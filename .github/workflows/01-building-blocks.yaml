name: 01 - building Blocks

on:
  push:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, World"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: succeed step
        run: |
          echo "i will succeed"
          exit 0
      - name: Say goodbye
        run: echo "goodbye"



